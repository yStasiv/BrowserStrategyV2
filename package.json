{
  "name": "browser-strategy-backend",
  "version": "1.0.0",
  "description": "Simple backend to record battle results for BrowserStrategy",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "migrate": "psql \"$DATABASE_URL\" -f migrations/create_matches.sql || echo 'Run psql manually with DATABASE_URL set'"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.11.0"
  }
}
